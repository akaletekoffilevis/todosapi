[ ] Refresh tokens
[ ] Pagination des tâches
[ ] Filtrage avancé
[ ] Catégories/Tags
[ ] Partage de tâches
[ ] Notifications
[ ] Tests unitaires (xUnit)
[ ] Logging avancé (Serilog)
[ ] Rate limiting
[ ] Cache (Redis)

**Pagination
[HttpGet]
public async Task<IActionResult> GetUserTasks([FromQuery] int page = 1, [FromQuery] int pageSize = 10)
{
    var todos = await _todoService.GetUserTodos(userId);
    var paged = todos.Skip((page - 1) * pageSize).Take(pageSize).ToList();
    return Ok(paged);
}

**uitlistaion frontend
// Inscription
const registerResponse = await fetch('http://localhost:5000/api/auth/register', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'user', password: 'pass' })
});

// Connexion
const loginResponse = await fetch('http://localhost:5000/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'user', password: 'pass' })
});
const { token } = await loginResponse.json();

// Récupérer les tâches
const tasksResponse = await fetch('http://localhost:5000/api/tasks', {
  headers: { 'Authorization': `Bearer ${token}` }
});
const tasks = await tasksResponse.json();

doc
-  [Microsoft - ASP.NET Core Docs](https://docs.microsoft.com/aspnet/core/)
-  [JWT.io - Learn JWT](https://jwt.io/introduction)
-  [Entity Framework Core Docs](https://docs.microsoft.com/ef/core/)
-  [OWASP Security](https://owasp.org/)
-  [Postman Learning](https://learning.postman.com/)



